'use babel';

import BreaditorAtomBridgeView from '../lib/breaditor-atom-bridge-view';

describe('BreaditorAtomBridgeView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
